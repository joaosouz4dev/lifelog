# Servidor
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
pyyaml==6.0.2
httpx==0.28.1

# Transcrição local
faster-whisper==1.1.1
# huggingface_hub/hub.py importa requests para baixar o modelo. Não vem como
# dependência declarada de ninguém, então no bundle do PyInstaller faltava e
# TODA transcrição falhava com "No module named 'requests'".
requests>=2.31

# VAD
onnxruntime==1.20.1
numpy==2.2.1

# Áudio
soundfile==0.13.0

# Captura no Windows (só o cliente precisa)
PyAudioWPatch==0.2.12.7; sys_platform == "win32"
pystray==0.19.5; sys_platform == "win32"
pycaw==20240210; sys_platform == "win32"
comtypes==1.4.8; sys_platform == "win32"
# pywin32 dá o mutex nomeado que impede duas capturas simultâneas —
# duas bandejas disputam o mesmo dispositivo de áudio e a mesma fila.
pywin32==308; sys_platform == "win32"
# Janela nativa da interface, sobre o WebView2 que já vem no Windows 11 —
# sem isto a interface só abre como aba do navegador.
pywebview==6.2.1; sys_platform == "win32"
# Usado para encerrar um servidor de versão antiga preso na porta depois de
# atualizar. Sem isso os segmentos ficam presos em 'pending'.
psutil>=5.9; sys_platform == "win32"

# Testes
pytest==8.3.4
pytest-asyncio==0.25.2
